# ----------------------------------------
# Fly.io Deployment Config
# ----------------------------------------
fly.toml
# Файл конфігурації для розгортання на Fly.io.
# Містить налаштування сервісів, портів, release_command і середовище.

# ----------------------------------------
# Environment Variables
# ----------------------------------------
.env
# Основний файл змінних середовища. Містить всі секрети (ключі, паролі, URL БД тощо).

.env.*
# Всі додаткові файли змінних середовища, наприклад:
# .env.production, .env.development, .env.test

!env.example
# Файл-шаблон env.example з прикладом змінних, який не ігнорується.
# Використовується для поширення серед команди, щоб вони створювали свій локальний .env

example.env
# Можливий альтернативний приклад файлу змінних середовища для локальної розробки
